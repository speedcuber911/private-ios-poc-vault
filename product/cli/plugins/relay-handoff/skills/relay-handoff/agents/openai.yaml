interface:
  display_name: "Relay Repository Sync"
  short_description: "Push repo work and sync Codex chats to Relay"
  default_prompt: "Use $relay-handoff to publish this repository's current task and sync its Codex sessions to Relay."
