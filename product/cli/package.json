{
  "name": "@relay/cli",
  "version": "0.1.4",
  "description": "Publish repository work and sync native Codex sessions to your Relay machine",
  "type": "module",
  "engines": { "node": ">=20" },
  "bin": { "relay": "bin/relay" },
  "files": ["bin", "src", "plugins", "dist/install.sh"],
  "scripts": { "test": "node --test test/*.test.mjs" },
  "license": "MIT"
}
