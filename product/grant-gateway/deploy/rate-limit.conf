# Shared-memory request limit used only by the grant-gateway virtual host on the
# broker host. Include once from the nginx http{} context (e.g.
# /etc/nginx/conf.d/relay-grant-gateway-rate-limit.conf).
limit_req_zone $binary_remote_addr zone=relay_grant_gateway_per_ip:10m rate=10r/s;
